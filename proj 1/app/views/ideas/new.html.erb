<!DOCTYPE html>
<html lang="en">

<head>
    <title>Innovations Corner</title>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">


    <!-- Bootstrap Core CSS -->
    <link href="app/assets/stylesheets/bootstrap.min.css" rel="stylesheet">

    <!-- Custom CSS -->
    <link href="app/assets/stylesheets/business-frontpage.css" rel="stylesheet">

</head>

<body>
    	<div id="bg">
        <div class="container" >
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="tagline">Innovations Corner</h1>
			<a href="/pages/index_student">
			<button type="button" class="btn btn-default pull-right">
			      <span class="glyphicon glyphicon-arrow-left">Back</span>
			</button>
			</a>
			<a href="/pages/index_student">
			<button type="button" class="btn btn-default pull-right">
			      <span class="glyphicon glyphicon-home"> Home</span>
			</button>
			</a>
                </div>
	 </div>
        </div>
	</div>
<div class="container">
  <h3>Your Previous Ideas were..</h3>           
  <table class="table">
    <thead>
      <tr>
        <th>Idea Title</th>
        <th>Category</th>
	<th>Status</th>
	<th>Action</th>

      </tr>
    </thead>
    <% if @ideas.any? %>
     <% @ideas.each do |idea| %>
      <tbody>
        <tr>
	        <td><%= idea.title %></td>
          <td><%= Category.find(idea.category_id).name.to_s %></td>
          <td><%= idea.status %></td>
          <td><%= link_to 'View', idea %></td>
        </tr>
      </tbody> 
     <% end %>
   <% end %>
  </table>
<div id="bg1">
<div class="row">
 <div class="col-lg-4">
	<h3>Post Your Ideas Here...</h3>
 </div>
</div>
</div>
</div>
<div class="row">
<div class="col-lg-offset-2 col-lg-8 col-lg-offset-2 " id="form1">
<%= form_for @idea do |f| %>

  <%= f.label :title, 'Title'%>
  <%= f.text_field :title %>
  <br>
  <%= f.label :catagory_id, "Category" %>
  <%= f.select(:category_id) do %>
    <% @category.each do |cat| %>
      <%= content_tag(:option, cat.name, value: cat.id) %>
    <% end %>
  <% end %>
  <br>
  <%= f.label :descrption %><br>
  <%= f.text_area :description %><br>

  <%= f.submit 'Post', class: "col-lg-offset-2 col-lg-8 col-lg-offset-2 " %>
<% end %>  
</div>
</div>
</body>
</html>

    


